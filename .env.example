# Docker host ports (container ports stay 8000 / 80)
# Change if these conflict with other services on your machine.
API_HOST_PORT=8001
WEB_HOST_PORT=5174

# Local dev (npm run dev) uses frontend port 5174 and API port 8000 by default.
